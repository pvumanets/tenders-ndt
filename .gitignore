# Секреты и окружение
.env
.env.*
!.env.example
# `.env.vps` — root SSH (matched by .env.*)

# Cookies / сессии
cookies*.txt
*.cookies

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Playwright
playwright-report/
test-results/

# Том документов (P5.5, локальный путь вне compose)
data/docs/

# Артефакты прогонов
runs/**/docs/
runs/**/*.xlsx
runs/**/*.xls
runs/**/*.csv
runs/**/*.json
runs/**/*.zip
runs/**/*.rar
runs/**/*.7z
!runs/.gitkeep

# Node / React (app/web)
node_modules/
app/web/dist/
*.tsbuildinfo

# IDE / OS
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

runs/_probe/

# Черновики зондов (не в origin)
_probe_*
