# Visual SoT — vendored from ndt-personal

Copied from `ndt-personal/apps/web` (theme + shell/column/card/drawer patterns, `FilterTriggerButton`).
Scout adapters live in `src/components/scout/`. Do not invent a parallel design system.
Filter menus: vertical Checkbox/Radio/Switch lists like `DispatchFilterMenu` — never wrapping chips.
