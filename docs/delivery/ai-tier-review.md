# ИИ-проверка тира (отдельный шаг после прогона)

**status:** draft  
**last-review-date:** 2026-08-29  
**hardening (042):** timeout/transport → `AiTierError` + model chain + commit per lot  
**owner lock (2026-08-27):** ИИ **не** при каждой загрузке; **отдельный шаг**; раздел «Разобрано с помощью ИИ»; журнал ошибок  
**owner lock (2026-08-26):** провайдер **provod.ai**; запасные **модели того же API**  
**owner lock (2026-08-28):** независимая классификация — **не** передаём `rules_tier` / `fit_reason` в модель  
**тиры:** [`fit-tiers.md`](./fit-tiers.md)  
**мастер-промпт (accepted):** [`ai-master-prompt.md`](./ai-master-prompt.md)  
**решения владельца:** [`../discovery/owner-decisions.md`](../discovery/owner-decisions.md)  
**lifecycle:** [`../discovery/inbox-lifecycle.md`](../discovery/inbox-lifecycle.md)  
**код:** задача [029](./tasks/029-tier-rules-and-ai.md) (**done**); wire промпта [039](./tasks/039-ai-master-prompt.md)

Этот файл — **канон будущей реализации**, не инструкция запускать ИИ из Cursor.

---

## Зачем

Правила score иногда путают **поставку оборудования** с **услугой НК**. Жёсткие правила ставят тир и **сразу кладут на доску**.

**Страховка поиска** (широкие query уровня E: «контроль сварн», «диагностирование»…) намеренно тянет **больше шума**. Владелец: ИИ подключаем **как раз чтобы классно разбирать мусор** — обучение, железо, нерелевант не должны оставаться в Горячих.

**ИИ** — по **команде оператора** (отдельный шаг), чтобы проверить/поправить и **видеть ошибки**, а не молча перезаписывать всё при прогоне.

---

## Два этапа (не один ingest)

```text
Прогон (правила)
  → tier L1|L2|L3 на доске
  → ai_reviewed = false (или null)

Оператор: «Разобрать с ИИ» (пакет или выбор)
  → provod.ai: title ± description ± customer (без rules_tier / fit_reason)
  → JSON: tier + reason_ru
  → ai_reviewed = true; раздел «Разобрано с помощью ИИ»
  → при ошибке: ai_error, tier правил сохраняется, запись в журнал
```

| Этап | Когда | Результат |
| --- | --- | --- |
| **1. Правила** | Каждый прогон | Тир L1/L2/L3 на доске |
| **2. ИИ** | **По команде** после прогона | Подтверждение/правка + `reason_ru`; видимость в отдельном разделе |

Повторный прогон того же лота: **обновление полей с площадки** если изменились (срок, цена, docs) — см. lifecycle. **ИИ не перезапускается** автоматически; `ai_reviewed` сохраняется, пока оператор не запустит заново.

`reason_ru` после ИИ — в drawer «Почему подходит» **всем** операторам.

---

## UI / продукт

| Элемент | Смысл |
| --- | --- |
| Вкладка **«Разобрано с помощью ИИ»** | Отдельная доска: `ai_reviewed=1`, колонки по `ai_tier`, chip переноса `L2 → L3` |
| Кнопка «Разобрать с ИИ» | **Только** на вкладке ИИ; очередь лотов без `ai_reviewed` |
| Вкладка **«Лоты»** | Колонки по **rules** (`rules_tier` / `tier`); ИИ не сдвигает колонки |
| «ИИ ошибся» | Фиксация в журнал (для улучшения промпта/правил); tier можно вернуть к правилам или ручной |
| Баннер при массовом сбое | Если ключ мёртв / обе модели упали — **явно**, не маскировать под «Смотреть» |

---

## Поля (целевая модель)

| Поле | Назначение |
| --- | --- |
| `ai_reviewed_at` | Когда прошёл ИИ (null = только правила) |
| `ai_tier` | Тир после ИИ (может совпадать с tier правил) |
| `ai_reason_ru` | Краткое объяснение для drawer |
| `ai_error` | Текст сбоя (parse fail, 429, нет ключа) |
| `rules_tier` | Тир до ИИ (для diff «было → стало») |

`manual_tier` из `lot_state` по-прежнему выше движка для UI.

---

## Провайдер: provod.ai

Публичный API (документация: [provod.ai docs](https://provod.ai/ru/docs), OpenAI- и Anthropic-совместимые контракты):

| Параметр | Значение |
| --- | --- |
| Base URL | `https://api.provod.ai` |
| Предпочтительный контракт для нашего worker | OpenAI-compatible `POST /v1/chat/completions` |
| Запасной контракт (тот же хост) | Anthropic-compatible `POST /v1/messages` (+ заголовок `anthropic-version`) |
| Каталог моделей | `GET /v1/models` (не выдумывать alias) |
| Авторизация | `Authorization: Bearer <ключ>` |
| Переменная окружения | `PROVOD_API_KEY` |

**Секреты:** ключ только в локальном `.env` / на VPS через `--sync`. Никогда в git, md, skills, чат-каноне.

При сбоях alias **не уходим на другой провайдер** — только другая модель на том же `api.provod.ai`.

---

## Цепочка моделей (один провайдер)

Default в коде ([`app/ai/provod.py`](../../app/ai/provod.py)); override: env `PROVOD_MODEL_CHAIN=a,b,c` (только alias с `api.provod.ai`, не выдумывать — сверять с `GET /v1/models`).

| Порядок | Alias | Роль | Smoke 2026-08-29 |
| --- | --- | --- | --- |
| 1 | `claude-sonnet-4-6` | Основная классификация RU-закупок | ~55 s |
| 2 | `openai-gpt-5-4` | Запасная через provod.ai | ~1.5 s |
| 3 | `gemini-2.5-flash` | Вторая запасная (из каталога) | ~0.9 s |

1. Запрос к #1 (timeout **120 с**).  
2. `AiTierError` (HTTP 4xx/5xx, плохой JSON) **или** `httpx` timeout/transport → следующая модель в цепочке.  
3. Все упали / нет ключа → **`ai_error`**, tier **остаётся от правил**, счётчик сбоев в Tech. **Не** silent L3.  
4. `run_ai_review` **коммитит каждый лот** — частичный успех пакета не откатывается.

---

## Контракт ответа модели

```json
{
  "tier": "L1",
  "reason_ru": "Кратко почему: услуга проведения НК, не поставка прибора"
}
```

- `tier` ∈ `L1` | `L2` | `L3`
- System prompt — SoT [`ai-master-prompt.md`](./ai-master-prompt.md) (**accepted**); **запрет** фильтра по классу опасности / допускам
- **Вход модели (только факты закупки):** `title`; опционально `customer_name`, `description` (~800 из `lot.raw`, если есть)
- **Не передаём в модель:** `rules_tier`, `fit_reason`, score, methods, теги regex (`rules_tier` в БД — только для UI-diff на вкладке «Лоты»)

---

## Что не делает ИИ

- Не ищет на площадках (это прогон httpx)
- Не решает просрочку
- Не создаёт дубликаты
- Не запускается автоматически при ingest (lock 2026-08-27)
- Не ходит в API вне provod.ai

---

## Включение на проде (2026-08-28)

1. `PROVOD_API_KEY=sk_…` в локальном `.env` (не в git).
2. `python scripts/vps-bootstrap.py --sync` — ключ уезжает в `/opt/tenders-ndt/.env` (`COPY_ENV_KEYS` в [`scripts/vps-bootstrap.py`](../../scripts/vps-bootstrap.py)).
3. Проверка на VPS (без печати ключа): `provod True` в api-контейнере.
4. Inbox → **«Разобрать с ИИ»** (или `POST /api/inbox/ai-review` с `{}` — все L1–L3 без `ai_reviewed`).
5. Прогон Rostender **без** ИИ; ИИ только после, вручную (owner lock).

**Стоимость (оценка, provod Sonnet 4.6):** ~250 input + ~80 output tok на лот. Доска после wipe #5 (037): **35 лотов ≈ 5–7 ₽** за полный разбор; только L1 (3) ≈ 0.5 ₽. Pool/noise в ИИ не идут.

**Статус:** первый живой прогон **2026-08-28** — 35 лотов (L1=3, L2=5, L3=27), `failed=0`, все с `ai_reason_ru`.

---

## Acceptance (для реализации 029)

- [ ] Прогон кладёт L1/L2/L3 на доску **без** вызова ИИ
- [ ] «Разобрать с ИИ» обрабатывает выбранные/очередь; успех → раздел «Разобрано с помощью ИИ»
- [ ] Эталоны fit-tiers на unit/mock правил
- [ ] Эталоны + mock HTTP на ИИ (без живого ключа в CI)
- [ ] **Мусор после широкого query (страховка E)** → не Горячие (L3 / noise по правилам+ИИ)
- [ ] Сбой ИИ → `ai_error` + баннер/счётчик; tier правил не меняется молча
- [ ] «ИИ ошибся» пишет в журнал
- [ ] Ключ только из env
