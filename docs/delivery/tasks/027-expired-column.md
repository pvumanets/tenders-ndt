---
id: "027"
type: task
status: backlog
phase: NEXT+
title: "Колонка «Просроченные» + авто-перенос + архив"
was: ""
---

# 027 — Колонка «Просроченные» + авто-перенос + архив

**route:** scout-product-manager → scout-architect → scout-frontend → scout-backend → scout-qa → scout-documentation-writer

**depends on:** канон [`../../discovery/inbox-lifecycle.md`](../../discovery/inbox-lifecycle.md) (`draft` → accept владельцем перед кодом)

**порядок:** первая в цепочке **027 → 028 → 029** (отдельный PR).

## Проблема

Лоты с прошедшим сроком подачи остаются в Горячих/Сильных/Смотреть: скрейп режет прошлое только при сборе, уже сохранённые в БД не двигаются.

## Решение

1. Четвёртая колонка доски **справа** — **«Просроченные»**.
2. Лот со сроком подачи &lt; сегодня (МСК) не показывается в L1–L3; виден в Просроченных; карточку можно открыть; бейдж **«Срок подачи вышел»**.
3. **Тихий суточный шаг** переносит просрочку; Inbox согласован с тем же правилом даты.
4. Сортировка: **свежие протухшие сверху**.
5. Горизонтальный скролл — **только у блока доски** (страница/шапка не уезжают).
6. Действие **«В архив»** (`board_hidden`): убрать со всех колонок; строка в БД остаётся; из drawer можно вернуть. «Скрыть ≠ архив» нет.

## Acceptance

- [ ] Четыре колонки; «Просроченные» справа
- [ ] Срок вчера и ранее → не в Горячих/Сильных/Смотреть; бейдж «Срок подачи вышел»
- [ ] Просроченную карточку можно открыть
- [ ] Суточный тихий шаг (+ согласованный показ Inbox)
- [ ] «В архив» / вернуть на доску
- [ ] Горизонтальный скролл только внутри блока доски
- [ ] Свежие протухшие сверху
- [ ] pytest/vitest на границу «сегодня / вчера» МСК

## Файлы (ожидаемые при коде)

- `app/web` — колонка, copy, бейдж, архив, scroll isolation
- `app/api/inbox.py` — выдача / группировка / `board_hidden`
- worker/cron — суточный шаг
- при необходимости поле в `lot_state` — отдельная миграция

## Out of scope

- Идемпотентный отчёт прогона ([028](./028-run-idempotent-report.md))
- Пересчёт тиров / ИИ ([029](./029-tier-rules-and-ai.md))
- Обновление полей живой карточки с площадки
- Ops wipe прода (описан в lifecycle; выполняется после выкладки 027–029)

## Links

- Discovery: [`../../discovery/inbox-lifecycle.md`](../../discovery/inbox-lifecycle.md)
- Index: [README](./README.md)
