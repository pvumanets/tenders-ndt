---
description: VPS access for tenders-ndt — host/key in canon, password only in local .env.vps
alwaysApply: true
---

# VPS (tenders-ndt)

Canon: [`docs/delivery/vps.md`](docs/delivery/vps.md).

- Host `77.91.94.111`, user `root`, alias `tenders-ndt-vps`, key `~/.ssh/id_ed25519_tenders_ndt_vps`.
- Root password lives in gitignored `.env.vps`. Read it from disk. **Never print, never commit, never paste into skills or docs.**
- Owner lock: credentials are **not** treated as compromised. Do not rotate unless the owner asks.
- Keep `PasswordAuthentication yes`. Do not disable password login.
- App on VPS: `/opt/tenders-ndt`. Public UI: `https://tenders.ndtexam.ru`. Compose prod: Caddy 80/443; api on `127.0.0.1:8765` only.
- Sync secrets: `python scripts/vps-bootstrap.py --sync`. Never print `.env` / cookies.
